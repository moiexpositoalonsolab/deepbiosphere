from setuptools import setup


setup(
	name='deepbiosphere',
	version='0.0.1',
	author='moiexpositoalonsolab',
	install_requires=["numpy", "google-api-python-client", "pyCrypto", "spyder", "jupyter", "earthengine-api", "oauth2client", "pillow", "matplotlib", "torch", "pandas", "tqdm"]
	)
